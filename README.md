List of command to be execute
```bash
npx create-next-app@latest .
npx shadcn@latest init
npm i @radix-ui/react-icons @tanstack/react-table lucide-react --legacy-peer-deps
npm install next-themes
npx shadcn@latest add calendar
npx shadcn@latest  add dropdown-menu button badge checkbox command popover separator select input table avatar 
npm i date-fns date-fns-tz react-day-picker zod --legacy-peer-deps  

npm i drizzle-orm pg dotenv --legacy-peer-deps  
npm i -D drizzle-kit tsx @types/pg --legacy-peer-deps  


 npm install express @types/express fs @types/fs path @types/node
 
 
frontend -  npm run dev
backed  - npm install --save-dev ts-node-dev
		  npx ts-node-dev server.ts



```
